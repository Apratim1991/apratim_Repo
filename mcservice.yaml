apiVersion: v1
kind: Service
metadata:
  name: vprocache01
spec:
  type: 
  selector:
    app: vpromc
  ports:
    - port: 11211         # Service Port
      targetPort: vpromc-port  # Container Port
      protocol: TCP
  type: ClusterIP  
